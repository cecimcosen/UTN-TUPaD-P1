# Analizar el siguiente programa y explicar con tus palabras qué es lo que realiza.

numeros = [8, 15, 3, 22, 7 ]
numeros.remove(max(numeros))
print(numeros)

# Lo que realiza el programa es quitar el número más grande a la lista, en este caso, sacará el 22.